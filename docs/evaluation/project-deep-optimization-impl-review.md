# 项目深度优化 实现锐评

生成日期：2026-06-08
评审范围：P0-1 清理已入库生成产物和本机缓存

## P0-1 清理已入库生成产物和本机缓存

### 综合评分

综合评分：**90 / 100**
评级：**A，放行**
一句话评价：**实现覆盖了计划中的止血目标，并补上了 CI 级防回流检查。**

### 评分总览

| 维度 | 分数 | 评价 |
| --- | ---: | --- |
| 计划符合度 | 95 / 100 | 按计划补齐 ignore、移除索引、保留本地文件，并增加防回流检查。 |
| 范围守纪 | 90 / 100 | 仅处理生成产物治理，没有混入业务逻辑。 |
| 代码质量 | 88 / 100 | 检查脚本简单直接，CI 接入位置清晰。 |
| 测试覆盖 | 85 / 100 | 本地执行了索引归零和脚本验证；真实 CI 运行需等 PR 环境验证。 |
| 主流对齐度 | 92 / 100 | 使用 `.gitignore`、`git rm --cached` 与 CI 检查，是常见仓库治理方式。 |
| 风险闭环 | 90 / 100 | 保留本地文件，避免误删；剩余风险是 GitHub Actions 尚未实跑。 |

### 一致点 / 亮点

- 已从索引移除 `.pnpm-store/`、coverage、uploads、`.umi-production/` 和 Playwright 报告产物。
- 根目录与子项目 `.gitignore` 都补齐了对应规则，减少不同工作目录下的误提交概率。
- 新增 `scripts/check-generated-artifacts.sh` 并接入现有 CI，后续产物回流会被阻断。

### 偏差 / 不足

- 计划建议“单独提交仓库清洁变更”，本步实现已准备为单独提交；提交动作属于本步收尾。
- 新增 CI 检查尚未在远端 Actions 中实跑，只能本地验证脚本行为。

### P0 致命项

- 无。

### 结论

- [x] 放行（综合分 ≥ 75 且无 P0）
- [ ] 需修订后重评

## P0-5 让 `UPLOAD_ROOT` 真正生效

### 综合评分

综合评分：**89 / 100**
评级：**B+，放行**
一句话评价：**本地文件存储已由配置驱动，并补上根目录逃逸保护和单元测试。**

### 评分总览

| 维度 | 分数 | 评价 |
| --- | ---: | --- |
| 计划符合度 | 90 / 100 | 完成配置映射、存储注入、路径校验和测试覆盖。 |
| 范围守纪 | 90 / 100 | 只改本地存储路径，不扩展对象存储或文件服务语义。 |
| 代码质量 | 88 / 100 | 路径解析集中在 `resolveStoragePath()`，读写删复用同一安全路径。 |
| 测试覆盖 | 88 / 100 | 新增存储单测，后端全量 unit/build 通过；缺少真实上传集成测试。 |
| 主流对齐度 | 90 / 100 | 使用 ConfigService 注入配置，并用 resolve/relative 防路径逃逸，符合常见本地存储做法。 |
| 风险闭环 | 88 / 100 | 默认开发路径保持 `uploads`，测试/灰度环境变量已存在；容器实测待后续验证。 |

### 一致点 / 亮点

- `UPLOAD_ROOT` 不再只是测试/CI 里的假配置，已经进入实际存储实现。
- 所有 storageKey 都经过根目录内校验，避免 `../` 写出上传目录。
- 单测验证文件确实写入配置目录，并验证逃逸路径被拒绝。

### 偏差 / 不足

- 没有把灰度清理脚本改为复用 Nest configuration 文件；脚本继续读取同一个环境变量，足以满足当前计划。
- 未运行依赖 PostgreSQL/Redis 的真实文件上传集成测试。

### P0 致命项

- 无。

### 结论

- [x] 放行（综合分 ≥ 75 且无 P0）
- [ ] 需修订后重评

## P0-4 统一 CORS 策略，删除双重配置

### 综合评分

综合评分：**90 / 100**
评级：**A，放行**
一句话评价：**CORS 已回到单一事实来源，冲突的 wildcard + credentials 守卫被移除。**

### 评分总览

| 维度 | 分数 | 评价 |
| --- | ---: | --- |
| 计划符合度 | 94 / 100 | 删除全局 CorsGuard，保留 enableCors，并将 origins 配置化。 |
| 范围守纪 | 92 / 100 | 只处理 CORS 配置，不混入认证或业务接口重构。 |
| 代码质量 | 90 / 100 | `main.ts` 读取 ConfigService，生产 wildcard 防护直接明确。 |
| 测试覆盖 | 84 / 100 | build/lint/引用扫描通过；缺少真实浏览器和预检请求验证。 |
| 主流对齐度 | 92 / 100 | 使用 NestJS `enableCors()` 管理跨域，是该栈常见做法。 |
| 风险闭环 | 88 / 100 | 保留本地默认白名单并禁止生产 wildcard + credentials；真实环境 origin 需部署时配置。 |

### 一致点 / 亮点

- 冲突的 `Access-Control-Allow-Origin: *` 与 credentials 组合已移除。
- CORS 来源从硬编码迁移到 `CORS_ORIGINS`，仍保留本地默认值。
- 生产环境对 wildcard 配置 fail-fast，避免静默带病启动。

### 偏差 / 不足

- 本步没有补 HTTP 预检自动化测试；当前通过代码路径、lint 和 build 验证。
- 未实测本地浏览器 SSE/上传跨域，后续全链路验证时补齐。

### P0 致命项

- 无。

### 结论

- [x] 放行（综合分 ≥ 75 且无 P0）
- [ ] 需修订后重评

## P0-3 为关键聊天入口补运行时 DTO 校验

### 综合评分

综合评分：**88 / 100**
评级：**B+，放行**
一句话评价：**v2 主聊天入口已具备运行时校验，非法 parts、role、附件数量和 toolChoice 会在 DTO 层被拦截。**

### 评分总览

| 维度 | 分数 | 评价 |
| --- | ---: | --- |
| 计划符合度 | 90 / 100 | 完成 v2 class DTO、多态 parts 校验、runtime 字段约束、toolChoice 引用检查，并补旧 chat DTO 基础校验。 |
| 范围守纪 | 88 / 100 | 聚焦入口校验，没有重写 Agent Runtime 或协议渲染。 |
| 代码质量 | 86 / 100 | DTO 约束集中、命名清晰；自定义 validator 直接服务本步目标。 |
| 测试覆盖 | 88 / 100 | 新增 11 个 DTO 单测，后端全量 unit 和 build 通过；HTTP e2e 尚未实跑。 |
| 主流对齐度 | 90 / 100 | 使用 NestJS `ValidationPipe` + class-validator/class-transformer，是当前栈的主流做法。 |
| 风险闭环 | 84 / 100 | 附件数量默认读取环境变量；仍需后续在真实 HTTP/e2e 中确认响应契约。 |

### 一致点 / 亮点

- `ChatStreamRequestV2` 从编译期 interface 升级为可被 `ValidationPipe` 处理的 class DTO。
- text/file/image/resource/command parts 分别校验，避免非法结构直接进入 Agent Runtime。
- `toolChoice` 从 runtime 失败前移到 DTO 层，且必须引用请求启用工具。
- 旧非流式 `ChatRequestDto` 也补上基础校验，避免留下明显入口短板。

### 偏差 / 不足

- `response.format` 仅保留基本字段，不在本步深挖 JSON schema 结构校验，避免扩大范围。
- 本步没有把附件数量从 `ConfigService` 注入 DTO，而是读取环境变量默认值；后续若配置需运行时热更新，应抽为专门 pipe。
- 尚未用真实 HTTP 请求验证 400 响应体，待后续 e2e/集成验证补齐。

### P0 致命项

- 无。

### 结论

- [x] 放行（综合分 ≥ 75 且无 P0）
- [ ] 需修订后重评

## P0-2 收紧认证边界，停止默认信任裸 `X-User-Id`

### 综合评分

综合评分：**86 / 100**
评级：**B+，放行**
一句话评价：**默认身份边界已经收紧，开发/测试 fallback 被显式配置隔离，仍需后续用真实 e2e 覆盖跨用户资源访问。**

### 评分总览

| 维度 | 分数 | 评价 |
| --- | ---: | --- |
| 计划符合度 | 90 / 100 | 覆盖 Guard、用户解析、前端默认用户、测试/灰度显式开关。 |
| 范围守纪 | 86 / 100 | 聚焦身份封口；仅额外接入 `/auth/me` 启动校验，属于计划内前端登录态收紧。 |
| 代码质量 | 84 / 100 | 复用现有 Guard、decorator、controller 分层；controller 中配置读取略重复，但比引入新抽象更克制。 |
| 测试覆盖 | 82 / 100 | 新增 Guard/util 单测，更新前端认证头测试；未本地跑依赖 DB/Redis 的 e2e。 |
| 主流对齐度 | 88 / 100 | 生产默认 token 认证、测试显式 fallback，符合常见后端身份边界做法。 |
| 风险闭环 | 86 / 100 | 灰度与测试通过环境变量显式保留旧 fixture 能力；真实跨用户资源隔离待 P1 闭环测试补强。 |

### 一致点 / 亮点

- `AuthGuard` 不再在缺少 token 时默认放行业务接口。
- `resolveUserId()` 默认不接受裸 header，消除了 controller 层误信任 header 的默认路径。
- 前端不再生成生产默认 demo 用户，未登录会进入登录页。
- 测试/灰度继续可用，但必须显式配置 `AUTH_ALLOW_HEADER_USER_ID=true`。

### 偏差 / 不足

- 计划中的“灰度和测试 fixture 改为真实 token 或显式开启测试 fallback”本步选择显式 fallback，真实 token fixture 暂不做，避免扩大改动面。
- 暂未实现“用户 A 无法通过 header 读取用户 B 资源”的真实 HTTP e2e；当前通过默认拒绝 header + service 既有隔离逻辑降低风险。
- controller 内部读取 `auth.allowHeaderUserId` 有少量重复，后续若认证策略继续扩展，可再沉淀为专门的身份解析服务。

### P0 致命项

- 无。

### 结论

- [x] 放行（综合分 ≥ 75 且无 P0）
- [ ] 需修订后重评
