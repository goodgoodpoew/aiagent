import { FC } from "react";
import BaseLayout from "./components/BaseLayout";

const Base: FC = () => {
  return <div className="base-index" style={{ height: '100%' }}>
    <BaseLayout />
  </div>;
};

export default Base;